'use client';import{useState}from'react';import{useRouter}from'next/navigation';export default function WoActions({id,status}:{id:number,status:string}){const r=useRouter();const[msg,setMsg]=useState('');async function call(url:string,body?:any){setMsg('Processing...');const x=await fetch(url,{method:'POST',headers:{'content-type':'application/json'},body:body?JSON.stringify(body):undefined});const j=await x.json();setMsg(x.ok?'Berhasil':j.error||'Gagal');if(x.ok)r.refresh()}return <div className="row">{status==='DRAFT'&&<button className="btn" onClick={()=>call(`/api/work-orders/${id}/submit`)}>Submit</button>}{status==='SUBMITTED'&&<><button className="btn green" onClick={()=>call(`/api/work-orders/${id}/approve`,{action:'APPROVE'})}>Approve</button><button className="btn red" onClick={()=>call(`/api/work-orders/${id}/approve`,{action:'REJECT'})}>Reject</button></>}{(status==='APPROVED'||status==='SYNC_ERROR')&&<button className="btn green" onClick={()=>call('/api/accurate/push-job',{workOrderId:id})}>Push to Accurate</button>}{msg&&<span className="small muted">{msg}</span>}</div>}
