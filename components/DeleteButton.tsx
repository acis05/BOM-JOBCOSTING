'use client';import{useRouter}from'next/navigation';import{useState}from'react';export default function DeleteButton({url,label='Hapus',confirmText='Yakin ingin menghapus data ini?'}:{url:string;label?:string;confirmText?:string}){const r=useRouter();const[msg,setMsg]=useState('');async function del(){if(!confirm(confirmText))return;const x=await fetch(url,{method:'DELETE'});const j=await x.json();if(!x.ok)return setMsg(j.error||'Gagal menghapus');r.refresh()}return <><button className="btn red compact" onClick={del}>{label}</button>{msg&&<span className="small danger-text">{msg}</span>}</>}
