'use client';import{useRouter}from'next/navigation';import{useState}from'react';export default function DeleteButton({url,label='Hapus'}:{url:string;label?:string}){const r=useRouter();const[msg,setMsg]=useState('');async function del(){if(!confirm('Yakin ingin menghapus data ini?'))return;const x=await fetch(url,{method:'DELETE'});const j=await x.json();if(!x.ok)return setMsg(j.error||'Gagal menghapus');r.refresh()}return <><button className="btn red compact" onClick={del}>{label}</button>{msg&&<span className="small danger-text">{msg}</span>}</>}
