'use client';import{useState}from'react';
export default function SubscriptionClient(){const[msg,setMsg]=useState('');async function request(planCode:string){setMsg('Mengirim permintaan...');const r=await fetch('/api/subscription/request',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({planCode})});const j=await r.json();setMsg(j.message||j.error)}return <div className="grid two" style={{marginTop:18}}><div className="card pricing"><h2>6 Bulan</h2><div className="price">Rp 1.500.000</div><p>✓ Maks. 5 user<br/>✓ Maks. 5 database Accurate<br/>✓ Semua fitur BOM, WO, Approval, Job Costing & Roll Over</p><button className="btn" onClick={()=>request('SIX_MONTHS')}>Ajukan Aktivasi 6 Bulan</button></div><div className="card pricing"><h2>1 Tahun</h2><div className="price">Rp 2.500.000</div><p>✓ Maks. 5 user<br/>✓ Maks. 5 database Accurate<br/>✓ Semua fitur BOM, WO, Approval, Job Costing & Roll Over</p><button className="btn green" onClick={()=>request('YEARLY')}>Ajukan Aktivasi 1 Tahun</button></div>{msg&&<div className="notice" style={{gridColumn:'1/-1'}}>{msg}</div>}</div>}
