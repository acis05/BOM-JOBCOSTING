'use client';import {useState} from'react';import {useRouter} from'next/navigation';import Link from'next/link';
export default function Page(){const r=useRouter();const[f,setF]=useState({companyName:'',displayName:'',email:'',phone:'',username:'',password:''});const[msg,setMsg]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setMsg('Membuat akun trial...');const x=await fetch('/api/auth/register-trial',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(f)});const j=await x.json();if(!x.ok)return setMsg(j.error||'Gagal');r.push('/');r.refresh()}return <main className="auth-page"><form className="card stack auth-card" onSubmit={submit}><img src="/logo.png" alt="logo" className="auth-logo"/><div><h2>Trial Gratis 3 Hari</h2><p className="muted">Coba seluruh fitur BOM-JOBCOSTING AOL. Maks. 5 user dan 5 database Accurate.</p></div><input className="input" placeholder="Nama Perusahaan" value={f.companyName} onChange={e=>setF({...f,companyName:e.target.value})}/><input className="input" placeholder="Nama PIC" value={f.displayName} onChange={e=>setF({...f,displayName:e.target.value})}/><input className="input" type="email" placeholder="Email" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/><input className="input" placeholder="No. WhatsApp / Telepon" value={f.phone} onChange={e=>setF({...f,phone:e.target.value})}/><input className="input" placeholder="Username" value={f.username} onChange={e=>setF({...f,username:e.target.value})}/><input className="input" type="password" placeholder="Password minimal 6 karakter" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/><button className="btn green">Mulai Trial 3 Hari</button>{msg&&<div className="notice">{msg}</div>}<div className="center small"><Link href="/login">Sudah punya akun? Login</Link></div></form></main>}
