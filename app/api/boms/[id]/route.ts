import {query} from '@/lib/db';export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;const [b,m,c]=await Promise.all([query<any>('SELECT * FROM boms WHERE id=$1',[id]),query<any>('SELECT * FROM bom_materials WHERE bom_id=$1 ORDER BY id',[id]),query<any>('SELECT * FROM bom_costs WHERE bom_id=$1 ORDER BY id',[id])]);if(!b.rows[0])return Response.json({error:'not found'},{status:404});return Response.json({...b.rows[0],materials:m.rows,costs:c.rows})}
