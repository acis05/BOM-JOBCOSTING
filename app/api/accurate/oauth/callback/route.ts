import {NextResponse} from 'next/server';import {query} from '@/lib/db';import {publicBaseUrl} from '@/lib/accurate';import {getCurrentUser} from '@/lib/auth';
export async function GET(req:Request){const me=await getCurrentUser();const u=new URL(req.url);const base=publicBaseUrl(u.origin);if(!me)return NextResponse.redirect(`${base}/login`);const code=u.searchParams.get('code');if(!code)return NextResponse.redirect(`${base}/accurate?error=no_code`);const id=process.env.ACCURATE_CLIENT_ID,secret=process.env.ACCURATE_CLIENT_SECRET,redirect=process.env.ACCURATE_REDIRECT_URI;if(!id||!secret||!redirect)return NextResponse.redirect(`${base}/accurate?error=config`);const form=new URLSearchParams({code,grant_type:'authorization_code',redirect_uri:redirect});const res=await fetch('https://account.accurate.id/oauth/token',{method:'POST',headers:{Authorization:`Basic ${Buffer.from(`${id}:${secret}`).toString('base64')}`,'Content-Type':'application/x-www-form-urlencoded'},body:form});const j=await res.json();if(!res.ok||!j.access_token)return NextResponse.redirect(`${base}/accurate?error=token`);const exp=new Date(Date.now()+Number(j.expires_in||1295999)*1000);await query(`UPDATE accurate_connections SET access_token=$2,refresh_token=$3,token_expires_at=$4,scope=$5,user_email=$6,updated_at=NOW() WHERE organization_id=$1`,[me.organization_id,j.access_token,j.refresh_token||null,exp,j.scope||null,j.user?.email||null]);const existing=await query<any>('SELECT id FROM accurate_connections WHERE organization_id=$1 LIMIT 1',[me.organization_id]);if(!existing.rows[0])await query(`INSERT INTO accurate_connections(organization_id,access_token,refresh_token,token_expires_at,scope,user_email,is_active) VALUES($1,$2,$3,$4,$5,$6,FALSE)`,[me.organization_id,j.access_token,j.refresh_token||null,exp,j.scope||null,j.user?.email||null]);return NextResponse.redirect(`${base}/accurate?connected=1`)}
