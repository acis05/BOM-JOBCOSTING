import {getConnection} from '@/lib/accurate';import {syncAccurateMasters} from '@/lib/accurate-sync';
export async function POST(){const c=await getConnection();if(!c?.api_host||!c?.session_id)return Response.json({error:'Pilih database Accurate terlebih dahulu.'},{status:400});try{const result=await syncAccurateMasters();return Response.json({ok:true,result,message:'Data barang, gudang, dan akun berhasil disinkronkan.'})}catch(e:any){return Response.json({error:e.message},{status:400})}}
