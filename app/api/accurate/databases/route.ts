import {requireApiPermission} from '@/lib/auth';
import{getConnection}from'@/lib/accurate';export async function GET(){const auth=await requireApiPermission('accurate.connect');if(!auth.ok)return auth.response;const c=await getConnection();if(!c?.access_token)return Response.json({error:'Belum connect Accurate'},{status:400});const r=await fetch('https://account.accurate.id/api/db-list.do',{headers:{Authorization:`Bearer ${c.access_token}`}});return new Response(await r.text(),{status:r.status,headers:{'content-type':'application/json'}})}
