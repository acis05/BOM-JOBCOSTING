import{query}from'@/lib/db';export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params;const r=await query<any>(`UPDATE work_orders SET status='SUBMITTED',updated_at=NOW() WHERE id=$1 AND status='DRAFT' RETURNING *`,[id]);if(!r.rows[0])return Response.json({error:'WO harus berstatus DRAFT'},{status:400});await query(`INSERT INTO approval_logs(work_order_id,action) VALUES($1,'SUBMITTED')`,[id]);return Response.json(r.rows[0])}
