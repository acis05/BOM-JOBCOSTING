'use client';import{useState}from'react';import{useRouter}from'next/navigation';
export default function Page(){const r=useRouter();const[u,setU]=useState('superadmin');const[p,setP]=useState('');const[msg,setMsg]=useState('');async function go(e:React.FormEvent){e.preventDefault();const x=await fetch('/api/admin/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({username:u,password:p})});const j=await x.json();if(!x.ok)return setMsg(j.error||'Login gagal');r.push('/admin');r.refresh()}return <main className="auth-page"><form className="card stack auth-card" onSubmit={go}><img src="/logo.png" className="auth-logo"/><h2>Super Admin</h2><p className="muted">Khusus pengelola BOM-JOBCOSTING AOL.</p><input className="input" value={u} onChange={e=>setU(e.target.value)} placeholder="Username admin"/><input className="input" type="password" value={p} onChange={e=>setP(e.target.value)} placeholder="Password"/><button className="btn">Masuk Admin</button>{msg&&<div className="notice">{msg}</div>}</form></main>}
