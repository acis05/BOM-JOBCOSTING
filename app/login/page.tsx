'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';import Link from 'next/link';
export default function LoginPage(){const r=useRouter();const[username,setUsername]=useState('');const[password,setPassword]=useState('');const[msg,setMsg]=useState('');async function login(e:React.FormEvent){e.preventDefault();setMsg('Masuk...');const x=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({username,password})});const j=await x.json();if(!x.ok)return setMsg(j.error||'Login gagal');r.push('/');r.refresh()}return <main className="auth-page"><div style={{width:'100%',maxWidth:980}}><div className="grid two"><form className="card stack auth-card" onSubmit={login}><img src="/logo.png" alt="BOM-JOBCOSTING AOL" className="auth-logo"/><h2>Login</h2><label>Username<input className="input" value={username} onChange={e=>setUsername(e.target.value)}/></label><label>Password<input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="btn green" type="submit">Masuk</button>{msg&&<div className="notice">{msg}</div>}<div className="center small"><Link href="/admin/login">Login Super Admin</Link></div></form><div className="card stack"><h2>Belum punya akun?</h2><p className="muted">Mulai trial gratis 3 hari. Tidak perlu kartu kredit.</p><div className="pricing-mini"><b>Trial 3 Hari</b><span>Maks. 5 user · Maks. 5 database Accurate</span></div><div className="pricing-mini"><b>6 Bulan — Rp 1.500.000</b><span>Aktivasi manual oleh admin</span></div><div className="pricing-mini"><b>1 Tahun — Rp 2.500.000</b><span>Aktivasi manual oleh admin</span></div><Link className="btn" href="/register">Buat Akun Trial</Link></div></div></div></main>}
