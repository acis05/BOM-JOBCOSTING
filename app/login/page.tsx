'use client';
import {useState} from 'react';
import {useRouter} from 'next/navigation';
export default function LoginPage(){const r=useRouter();const[username,setUsername]=useState('admin');const[password,setPassword]=useState('');const[msg,setMsg]=useState('');async function login(e:React.FormEvent){e.preventDefault();setMsg('Masuk...');const x=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({username,password})});const j=await x.json();if(!x.ok)return setMsg(j.error||'Login gagal');r.push('/');r.refresh()}return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',padding:20}}><form className="card stack" onSubmit={login} style={{width:'100%',maxWidth:420}}><img src="/logo.png" alt="BOM-JOBCOSTING AOL" style={{width:'100%',maxWidth:340,margin:'0 auto 8px'}}/><h2 style={{margin:'0 0 6px'}}>Login</h2><label>Username<input className="input" value={username} onChange={e=>setUsername(e.target.value)}/></label><label>Password<input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="btn green" type="submit">Masuk</button>{msg&&<div className="muted small">{msg}</div>}</form></main>}
