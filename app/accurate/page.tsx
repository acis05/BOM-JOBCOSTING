import Header from'@/components/Header';import AccuratePanel from'@/components/AccuratePanel';import{getConnection}from'@/lib/accurate';export const dynamic='force-dynamic';export default async function Page(){const c=await getConnection();return <><Header title="Accurate Online" subtitle="OAuth, database selection, dan adapter Job Costing."/><AccuratePanel connected={!!c?.access_token} databaseAlias={c?.database_alias} email={c?.user_email}/><div className="card" style={{marginTop:16}}><h2 className="section-title">Job Costing Adapter</h2><div className="notice">Setelah koneksi berhasil, isi <span className="code">ACCURATE_JOB_SAVE_PATH</span> di Railway dengan endpoint <b>Pekerjaan Pesanan / save</b> dari Developer API Accurate Anda. Source sudah menyiapkan mapping WO materials + other costs ke form-urlencoded dan header Bearer + X-Session-ID.</div></div></>}
