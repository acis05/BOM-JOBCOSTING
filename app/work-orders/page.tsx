import Link from'next/link';import Header from'@/components/Header';import WoActions from'@/components/WoActions';import{query}from'@/lib/db';export const dynamic='force-dynamic';export default async function Page(){const r=await query<any>('SELECT * FROM work_orders ORDER BY created_at DESC');return <><Header title="Work Orders" subtitle="Draft → Submit → Approve → Sync to Accurate"/><div className="card"><div className="right"><Link className="btn green" href="/work-orders/new">+ New WO</Link></div><table className="table"><thead><tr><th>WO</th><th>Date</th><th>Product</th><th>Qty</th><th>Status</th><th>Action</th></tr></thead><tbody>{r.rows.map((x:any)=><tr key={x.id}><td className="code">{x.wo_no}</td><td>{String(x.wo_date).slice(0,10)}</td><td>{x.product_name}</td><td>{Number(x.planned_qty)}</td><td><span className={`status ${x.status}`}>{x.status}</span>{x.accurate_job_no&&<div className="small muted">{x.accurate_job_no}</div>}</td><td><WoActions id={x.id} status={x.status}/></td></tr>)}</tbody></table></div></>}
