import Link from 'next/link';import Header from '@/components/Header';import{query}from'@/lib/db';export const dynamic='force-dynamic';export default async function Page(){const r=await query<any>('SELECT * FROM boms ORDER BY created_at DESC');return <><Header title="BOM / Formula" subtitle="Template material dan biaya pekerjaan."/><div className="card"><div className="right"><Link className="btn green" href="/boms/new">+ New BOM</Link></div><table className="table"><thead><tr><th>BOM</th><th>Formula</th><th>Product</th><th>Output</th><th>Status</th></tr></thead><tbody>{r.rows.map((x:any)=><tr key={x.id}><td className="code">{x.bom_no}</td><td>{x.name}</td><td>{x.product_item_no} — {x.product_name}</td><td>{Number(x.output_qty)}</td><td>{x.status}</td></tr>)}</tbody></table></div></>}
