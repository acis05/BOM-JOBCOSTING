import {requirePagePermission} from '@/lib/auth';
import Header from '@/components/Header';import BomForm from '@/components/BomForm';import{query}from'@/lib/db';export const dynamic='force-dynamic';export default async function Page(){const user=await requirePagePermission('bom.create');const [i,a]=await Promise.all([query<any>('SELECT item_no,name,unit FROM items_cache WHERE organization_id=$1 ORDER BY item_no',[user.organization_id]),query<any>("SELECT account_no,name,account_type FROM accounts_cache WHERE organization_id=$1 AND account_no IS NOT NULL ORDER BY account_no",[user.organization_id])]);return <><Header title="New BOM" subtitle="Formula standar sebelum dibuat menjadi Work Order."/><BomForm items={i.rows} accounts={a.rows}/></>}
