import Header from '@/components/Header';import BomForm from '@/components/BomForm';import{query}from'@/lib/db';export const dynamic='force-dynamic';export default async function Page(){const [i,a]=await Promise.all([query<any>('SELECT item_no,name,unit FROM items_cache ORDER BY item_no'),query<any>("SELECT account_no,name,account_type FROM accounts_cache WHERE account_no IS NOT NULL ORDER BY account_no")]);return <><Header title="New BOM" subtitle="Formula standar sebelum dibuat menjadi Work Order."/><BomForm items={i.rows} accounts={a.rows}/></>}
